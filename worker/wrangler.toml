name = "personal-server-router"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# This worker serves as a fallback for *.myvana.app requests that don't
# match a Cloudflare Tunnel. Active servers route via tunnel CNAME records.
# No secrets or KV bindings needed.
